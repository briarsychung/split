import { Interactive } from './interactive.mjs';

class Button extends Interactive {
    constructor(url, pos, dim) {
        super(url, pos, dim);
    }

    trigger() {
        // idk lmao
    }
}

export { Button };